[project]
name = "artiq_http_client"
version = "0.3.0"
description = "Python client SDK for ARTIQ HTTP"
requires-python = ">=3.11"
dependencies = ["httpx>=0.24.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
