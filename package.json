{
  "name": "artiq-http",
  "version": "0.4.0",
  "scripts": {
    "install": "cd frontend && npm install",
    "frontend": "cd frontend && npm start",
    "backend": "poetry run uvicorn artiq_http.main:fastapi_app --reload",
    "openapi:export": "poetry run python -c \"import json; from artiq_http.api import app; print(json.dumps(app.openapi()))\" 2>/dev/null | grep -v '^Production mode' > fern/openapi/openapi.json",
    "sdk:generate": "fern generate --group python-sdk",
    "sdk:update": "npm run openapi:export && npm run sdk:generate",
    "static:update": "(rm -r artiq_http/static || true) && cd frontend && npm run build && mv dist ../artiq_http/static",
    "build": "npm run static:update",
    "clean": "git clean -xdf --exclude venv --exclude node_modules --exclude .env"
  }
}
