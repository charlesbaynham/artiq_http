# Bundled ARTIQ SDK artifacts

This directory contains distributable artifacts built from `sdk/`.

To refresh these files, run:

```bash
./scripts/update_claude_plugin_sdk_resources.sh
```

The latest copied artifact filenames are recorded in `LATEST_SDK.txt`.
